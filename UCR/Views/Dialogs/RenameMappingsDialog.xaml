<UserControl x:Class="HidWizards.UCR.Views.Dialogs.RenameMappingsDialog"
             Style="{StaticResource UcrDialogRoot}"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:controls="clr-namespace:HidWizards.UCR.Views.Controls"
             MinWidth="620" MinHeight="300" Loaded="RenameMappingsDialog_OnLoaded">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Rename mappings" Style="{StaticResource MaterialDesignTitleTextBlock}" FontSize="20"/>
        <TextBlock Grid.Row="1" Margin="0 7 0 12" Opacity="0.72" TextWrapping="Wrap"
                   Text="Edit the names in one pass. Tab moves directly to the next mapping instead of opening a separate dialog for every card."/>
        <ScrollViewer Grid.Row="2" MaxHeight="520" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Items}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0 4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="34"/>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{Binding Number, StringFormat={}#{0}}" VerticalAlignment="Center" Opacity="0.50"/>
                            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0 0 10 0">
                                <ItemsControl ItemsSource="{Binding Mapping.CollapsedInputVisuals}">
                                    <ItemsControl.ItemsPanel><ItemsPanelTemplate><StackPanel Orientation="Horizontal"/></ItemsPanelTemplate></ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal" Margin="0 0 3 0">
                                                <Border Width="27" Height="18" Margin="0 0 3 0" Padding="3 0" CornerRadius="3" Background="#1B1B1B"
                                                        BorderBrush="{Binding Device.AccentBrush}" BorderThickness="1" ToolTip="{Binding Device.ToolTip}">
                                                    <Border.Style>
                                                        <Style TargetType="Border">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding ShowDeviceBadge}" Value="True"><Setter Property="Visibility" Value="Visible"/></DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Border.Style>
                                                    <TextBlock Text="{Binding Device.BadgeText}" Foreground="{Binding Device.AccentBrush}" FontSize="9.5"
                                                               FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                                <controls:ControlGlyphControl Width="25" Height="18" Margin="2 0 0 0" Kind="{Binding ControlKind}" AccentBrush="{Binding ControlBrush}" Label="{Binding ControlLabel}" ToolTip="{Binding ToolTip}"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                                <TextBlock Text="→" Margin="3 0" Opacity="0.45" VerticalAlignment="Center"/>
                                <ItemsControl ItemsSource="{Binding Mapping.CollapsedOutputVisuals}">
                                    <ItemsControl.ItemsPanel><ItemsPanelTemplate><StackPanel Orientation="Horizontal"/></ItemsPanelTemplate></ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal" Margin="0 0 3 0">
                                                <Border Width="27" Height="18" Margin="0 0 3 0" Padding="3 0" CornerRadius="3" Background="#1B1B1B"
                                                        BorderBrush="{Binding Device.AccentBrush}" BorderThickness="1" ToolTip="{Binding Device.ToolTip}">
                                                    <Border.Style>
                                                        <Style TargetType="Border">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding ShowDeviceBadge}" Value="True"><Setter Property="Visibility" Value="Visible"/></DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Border.Style>
                                                    <TextBlock Text="{Binding Device.BadgeText}" Foreground="{Binding Device.AccentBrush}" FontSize="9.5"
                                                               FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                                <controls:ControlGlyphControl Width="25" Height="18" Margin="2 0 0 0" Kind="{Binding ControlKind}" AccentBrush="{Binding ControlBrush}" Label="{Binding ControlLabel}" ToolTip="{Binding ToolTip}"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                            <TextBox Grid.Column="2" Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                     MinWidth="320" PreviewKeyDown="MappingName_OnPreviewKeyDown"/>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0 16 0 0">
            <Button Content="SAVE NAMES" IsDefault="True" Style="{StaticResource MaterialDesignRaisedButton}"
                    Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}" CommandParameter="{Binding ViewModel}"/>
            <Button Content="CANCEL" IsCancel="True" Margin="8 0 0 0" Style="{StaticResource MaterialDesignFlatButton}"
                    Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
